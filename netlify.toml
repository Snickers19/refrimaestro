[build]
  command = "pip install -r requirements.txt && flet publish"
  publish = "build/web"

[build.environment]
  PYTHON_VERSION = "3.10"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
